# Vendored opencode TUI+server stack — dependencies and build artifacts are
# never committed; run `bun install` here to materialize them.
node_modules/
dist/
build/
.turbo/
*.tsbuildinfo
.next/
*.log
node_modules
